<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<section xmlns="http://docbook.org/ns/docbook" version="5.0" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e" xml:lang="en-US">
<title>blink</title>
<indexterm><primary>blink</primary></indexterm>

<para><link xlink:href="https://ci.appveyor.com/project/crosire/blink"></link></para>

<para>A tool that lets you edit the source code of a Windows C++ application live, while it is running, without having to go through the usual compile-and-restart cycle. It is effectivly a runtime linker that detects changes to source code files, compiles them and links them back into the running application, while patching all updated functions to jump to the new code.</para>

<para>In contrast to similar projects, <emphasis role="bold">blink does not require you to modify your C++ project in any way</emphasis>. In fact it works on all x86 and x86-64 applications as long as they were compiled with debug symbols (so that a PDB file is created) and you have their source code somewhere on your system.</para>

<para></para>

<para>There now is a library which implements the same concept as blink but for Unix based systems: <link xlink:href="https://github.com/ddovod/jet-live">jet-live</link></para>
<section xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e_1autotoc_md0">
<title>Build</title>
<para>There are no dependencies, blink is fully standalone (apart from the Windows SDK). You only need a C++17 compliant version of MSVC 2017. Then just build the provided Visual Studio solution.</para>

<para>A quick overview of what each of the source code files contain:</para>

<para><informaltable frame="all">
    <tgroup cols="2" align="left" colsep="1" rowsep="1">
      <colspec colname='c1'/>
      <colspec colname='c2'/>
<thead>
      <row  class='markdownTableHead'>
<entry>
<para>File   </para>
</entry><entry>
<para>Description    </para>
</entry></row>
</thead><tbody>
      <row  class='markdownTableRowOdd'>
<entry>
<para><link xlink:href="source/blink.cpp">blink.cpp</link>   </para>
</entry><entry>
<para>Application logic, filesystem watcher, main loop    </para>
</entry></row>
      <row  class='markdownTableRowEven'>
<entry>
<para><link xlink:href="source/blink_linker.cpp">blink_linker.cpp</link>   </para>
</entry><entry>
<para>COFF loader and linker, symbol resolving, function patching    </para>
</entry></row>
      <row  class='markdownTableRowOdd'>
<entry>
<para><link xlink:href="source/main.cpp">main.cpp</link>   </para>
</entry><entry>
<para>Main entry point, remote thread injection, console output loop    </para>
</entry></row>
      <row  class='markdownTableRowEven'>
<entry>
<para><link xlink:href="source/coff_reader.cpp">coff_reader.cpp</link>   </para>
</entry><entry>
<para>Abstraction around COFF to deal with extended OBJ files (<computeroutput>/bigobj</computeroutput>)    </para>
</entry></row>
      <row  class='markdownTableRowOdd'>
<entry>
<para><link xlink:href="source/msf_reader.cpp">msf_reader.cpp</link>   </para>
</entry><entry>
<para>Parser for the MSF file format (which PDB files are based on)    </para>
</entry></row>
      <row  class='markdownTableRowEven'>
<entry>
<para><link xlink:href="source/pdb_reader.cpp">pdb_reader.cpp</link>   </para>
</entry><entry>
<para>Parser for the Microsoft PDB file format   </para>
</entry></row>
    </tbody>
    </tgroup>
</informaltable>
</para>
</section>
<section xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e_1autotoc_md1">
<title>Usage</title>
<para>Once you build blink, there are a couple of ways to use it: 1) Either launch your application via blink:\ <computeroutput>blink.exe foo.exe -arguments</computeroutput> 2) Or attach blink to an already running application:\ <computeroutput>blink.exe PID</computeroutput> where PID is the process ID to attach to 3) Add the <computeroutput>--no-compile</computeroutput> option before the other arguments to make blink watch for modifications to object files, rather than watching for and compiling the source files:\ <computeroutput>blink.exe --no-compile PID</computeroutput></para>

<para>Now simply do changes to any of the source code files of your application and see how they are reflected right away. Keep in mind that since blink patches on a function-level, you need a function that is repeatedly called, since <emphasis role="bold">changes are only made visible the next time a function is entered</emphasis>.</para>

<para>If blink has trouble finding the right compilation command-line for your project or you get a <computeroutput>cl.exe not found</computeroutput> error, make sure to check your build is generating PDB files with the <computeroutput>/ZI</computeroutput> compile option!</para>

<para>Optionally, if you define the following functions in your application, blink will call them before and after linking, which can be used to synchronize or save/restore application state: <literallayout><computeroutput>&#32;++
extern&#32;&quot;C&quot;&#32;void&#32;__blink_sync(const&#32;char&#32;*source_file);
extern&#32;&quot;C&quot;&#32;void&#32;__blink_release(const&#32;char&#32;*source_file,&#32;bool&#32;success);
</computeroutput></literallayout></para>
</section>
<section xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e_1autotoc_md2">
<title>Concept</title>
<para>Here is a quick run-down on how blink works:</para>

<para>When you attach it to an application it will start by finding the path to the matching PDB (program debug database) file generated by the compiler. This information is stored in the executable headers. After finding said file it parses it to obtain the list of source code files that were used to build the application and initialize the symbol table with all internal and external symbols of the application. Then it figures out the common parent path of all user source code files and starts listening for file modifications on that path.</para>

<para>Every time a change is registered, blink looks for the compilation command that was used to compile that source file in the application debug information, executes it to get a new object file reflecting the change and then links that object file into the application process.</para>

<para>The linking for the most part works like a traditional linker would, except that it also replicates some of what the Windows loader would do when you launch an executable, so that it can perform at runtime, rather than at compile-time. It parses the object file (which is in COFF format) and allocates and copies all necessary sections into the application process memory space. It then finds the symbol table of the object file and resolves all of them using symbol information that was previously extracted from the PDB file. The state of global variables is preserved by using the address of the existing symbols, so that the new code references the existing memory, rather than new one. Afterwards the relocation table of the object file is used to adjust all address references in memory to point to the correct addresses. And finally, blink writes a x86 jump instruction to the beginning of all functions in the application that are also part of the new code that was just loaded. This jump targets the new functions, so that every time the old function is called, the call is immediately redirected to the new code.</para>
</section>
<section xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e_1autotoc_md3">
<title>Contributing</title>
<para>Any contributions to the project are welcomed, it&apos;s recommended to use GitHub <link xlink:href="https://help.github.com/articles/using-pull-requests/">pull requests</link>.</para>
</section>
<section xml:id="_md__b_l_i_n_k_2_r_e_a_d_m_e_1autotoc_md4">
<title>License</title>
<para>All source code in this repository is licensed under a <link linkend="_md__b_l_i_n_k_2_l_i_c_e_n_s_e">BSD 2-clause license</link>. </para>
</section>
</section>
